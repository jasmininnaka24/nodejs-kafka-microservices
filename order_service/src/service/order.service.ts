export const CreateOrder = async (input: any) => {
    return { message: "Created order from service" }
}

export const GetOrder = async (input: any) => {
    return { message: "Extracted order from service" }
}

export const GetOrders = async (input: any) => {
    return { message: "Extracted orders from service" }
}

export const DeleteOrder = async (input: any) => {
    return { message: "Deleted order from service" }
}